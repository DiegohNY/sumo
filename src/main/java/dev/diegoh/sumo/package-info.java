/**
 * Sumo — a multi-arena push-battle minigame for Spigot/Paper/Purpur.
 *
 * <p>{@link dev.diegoh.sumo.SumoPlugin} is the entry point. On startup it builds every service once
 * and connects them together (the "composition root"); on shutdown it closes them. Start reading
 * there, then follow the package that owns the feature you care about. See {@code ARCHITECTURE.md}
 * in the project root for a map of how the packages fit together.
 */
package dev.diegoh.sumo;
